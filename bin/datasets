#!/usr/bin/env node

/**
 * Module dependencies.
 */
const DatasetManager = require('../lib/manager/dataset_manager');

/**
 * Start dataset managing process
 */
var manager = new DatasetManager();
manager.manage();