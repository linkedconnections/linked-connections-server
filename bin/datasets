#!/usr/bin/env node

/**
 * Module dependencies.
 */
const DataserManager = require('../src/manager/dataset_manager');

/**
 * Start dataset managing process
 */
var manager = new DataserManager();
manager.manage();